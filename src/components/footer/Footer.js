import styles from "../../../styles/Home.module.css";

const Footer = () => (
    <footer className={styles.footer}>
        <p>&copy; Built by LARBI ISHAK while learning NextJs</p>
    </footer>
);

export default Footer;
